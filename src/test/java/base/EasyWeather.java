package base;

import core.MobilePageBase;
import io.appium.java_client.AppiumDriver;

public class EasyWeather extends MobilePageBase {

    public EasyWeather(AppiumDriver driver) {
        super(driver);
    }
}
